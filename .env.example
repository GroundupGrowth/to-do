NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Postmark Inbound webhook shared secret.
# Generate a random string and set the same value in:
#  - your Vercel project env vars, and
#  - the `?token=<this>` query param on the Postmark Inbound Webhook URL.
POSTMARK_INBOUND_SECRET=
